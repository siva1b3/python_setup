


curl -X PUT http://localhost:9200/test-index \
  -H "Content-Type: application/json" \
  -d '{"settings": {"number_of_replicas": 0}}'

curl -X POST http://localhost:9200/test-index/_doc \
  -H "Content-Type: application/json" \
  -d '{"name": "test", "status": "ok"}'

Then click Reload in Dejavu UI — do not change the URL or index field, just reload.


URL:     http://publicipaws:9200
Index:   test-index

