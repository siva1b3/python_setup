name: etl-pipeline-elasticsearch

networks:
  etl-network:
    name: etl-network
    external: true

volumes:
  elasticsearch-data:
    name: elasticsearch-data

services:

  # ─── Elasticsearch 9.3.2 — Single-Node ──────────────────────────────────────
  etl-elasticsearch:
    image: elasticsearch:9.3.2
    container_name: etl-elasticsearch
    restart: unless-stopped
    networks:
      - etl-network
    ports:
      - "9200:9200"
    volumes:
      - elasticsearch-data:/usr/share/elasticsearch/data
    environment:
      discovery.type: single-node
      xpack.security.enabled: "false"
      ES_JAVA_OPTS: "-Xms256m -Xmx256m"
      http.port: "9200"
      http.max_content_length: 10MB
      http.cors.enabled: "true"
      http.cors.allow-origin: "http://localhost:1358,http://127.0.0.1:1358"
      http.cors.allow-headers: "X-Requested-With,X-Auth-Token,Content-Type,Content-Length,Authorization"
      http.cors.allow-credentials: "true"
      network.publish_host: localhost
    healthcheck:
      test: [ "CMD-SHELL", "curl -f http://localhost:9200/_cluster/health || exit 1" ]
      interval: 20s
      timeout: 10s
      retries: 10
      start_period: 60s

  # ─── Dejavu 3.10.0 — Elasticsearch UI ───────────────────────────────────────
  etl-dejavu:
    image: appbaseio/dejavu:3.10.0
    container_name: etl-dejavu
    restart: unless-stopped
    networks:
      - etl-network
    ports:
      - "1358:1358"
    depends_on:
      etl-elasticsearch:
        condition: service_healthy
